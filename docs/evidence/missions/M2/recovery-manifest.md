# M2 Commercial Trust Loop — WIP recovery manifest

Reconstruction of the uncommitted Codex working tree that was lost when the temporary
worktree `/tmp/td-agent-m2-commercial` was deleted before any commit was created.

## Sources

| Item | Value |
| --- | --- |
| Session journal | `~/.codex/sessions/2026/05/27/rollout-2026-05-27T14-11-17-019e6921-b611-73e2-b4bf-145243388db0.jsonl` (48 172 lines) |
| Extracted operations | `m2-recovery-operations.jsonl` (697 operations) |
| Recovery branch | `recovery/m2-commercial-reconstructed` |
| Base HEAD | `a7b085106ab732722509e22671d36c3d697ddd53` |
| Path remap | `/tmp/td-agent-m2-commercial` and `/private/tmp/td-agent-m2-commercial` → `/Users/silkwayrally/TD_agent_m2_recovered` |

## Operation census

| Kind | Count | Disposition |
| --- | --- | --- |
| `apply_patch` | 116 | 106 replayed, 10 deliberately skipped (see below) |
| `recorded_change` | 192 | used as ground truth, not replayed |
| `shell_command` | 389 | classified below; 10 replayed |
| **Total** | **697** | |

### `shell_command` classification

| Class | Count | Replayed? |
| --- | --- | --- |
| Diagnostic / read-only (`rg`, `sed`, `nl`, `git status`, `git diff`, `wc`, `cat`) | 254 | No — no filesystem effect |
| Test / verification runs (`npm test`, `node --test`, e2e, Playwright, axe) | 84 | No — re-run fresh as recovery gates |
| Other (mixed pipelines, `printf` summaries) | 15 | No — no filesystem effect on tracked files |
| Local DB provisioning (`createdb`, `psql`, `prisma migrate deploy`) | 12 | No — re-provisioned fresh for the gates |
| Prisma tooling (`prisma format`, `prisma generate`, `prisma migrate diff`) | 10 | **Yes** — reproduced in place, see below |
| Dev server (`npm run dev` / `start` / `build`) | 9 | No |
| Local test-fixture env (`openssl rand`, `/tmp/td-agent-m2-e2e.env`) | 3 | No — regenerated locally; contained only synthetic fixture credentials, never production secrets |
| `npm ci` / `npm install` | 2 | Once, to restore the lockfile toolchain (Prisma 5.22.0) |

No production commands, deployment commands, resource cleanup, or real-secret operations
were executed during recovery.

### Files created via heredoc / `cat` / `tee`

None. Every source file in the WIP originated from `apply_patch`. The only two
tool-generated files are Prisma artifacts:

* `prisma/schema.prisma` — reformatted in place by `prisma format` at journal
  sequences 63, 72 and 80.
* `prisma/migrations/20260729170000_m2_commercial_trust_loop/migration.sql` —
  generated by `prisma migrate diff --from-schema-datamodel … --script` at sequence 72
  (257 lines) and regenerated at sequence 80 (269 lines), then hand-patched.

## Skipped `apply_patch` operations

Ten patches produced **no** `recorded_change` in the journal — Codex's own applier
rejected them and Codex immediately reissued a corrected patch. Replaying them would
have diverged from the real working tree, so they are intentionally skipped.

| Rejected seq | Target(s) | Superseded by |
| --- | --- | --- |
| 68 | `prisma/schema.prisma` | 70 |
| 75 | `prisma/schema.prisma` | 78 |
| 108 | `lib/commercialQuoteService.ts` | 110 |
| 138 | `app/co/[code]/CoView.module.css` | 140 |
| 175 | 7 files (audit, catalog routes, catalog manager, quote pages) | 177 + 181 |
| 220 | `tests/integration/_setup.ts`, `QuoteBuilder.module.css` | 221 + 223 |
| 234 | `migration.sql`, `tests/integration/_setup.ts` | 235 + 237 |
| 362 | `QuoteBuilder.tsx`, `CaseRowActions.tsx`, `CasesList.tsx` | 364 |
| 419 | catalog routes, `CustomCatalogManager.tsx`, `migration.sql` | 421 |
| 588 | `commercialQuote.ts`, `commercialQuoteService.ts`, `QuoteBuilder.tsx`, `commercialQuote.test.ts` | 606 |

## Recovery result

| Metric | Value |
| --- | --- |
| Operations found in journal | 697 |
| File mutations replayed automatically | 195 (add / update / delete sub-operations) |
| Replayed with exact context match | 195 / 195 |
| Required fuzzy matching | 0 |
| Required manual reconstruction | 0 |
| Failed to apply | 0 |
| Intentionally not executed | 10 rejected patches + 379 non-mutating shell commands |

### Independent verification

Each recovered file was rebuilt a second time, from the `recorded_change` stream alone
(full content for additions, unified diffs for updates), starting from the base blob in
`HEAD`, and compared byte-for-byte with the replayed working tree.

* **65 / 65** directly reconstructible paths matched byte-for-byte.
* `prisma/schema.prisma` — verified indirectly: reverse-applying recorded changes 87 and
  385 reproduces the sequence-80 schema, which regenerates a 269-line `migration.sql`
  identical to the sequence-80 baseline. Codex's own `git diff --stat` at sequence 628
  reports `prisma/schema.prisma | 333 +++++++++++--`; the recovered tree reports the
  same 333 insertions.
* `prisma/migrations/…/migration.sql` — the recorded diff chain (82, 84, 86, 236, 383,
  425, 647) applied to the regenerated sequence-80 baseline reproduces the recovered
  file exactly (sole difference: the trailing newline, which `patch(1)` drops).

### Reconciliation with Codex's last logged `git diff --stat` (sequence 628)

Every file untouched after sequence 628 has identical insertion/deletion counts. The
only differences are the five files edited afterwards, plus one new file:

| File | seq 628 | recovered | Cause |
| --- | --- | --- | --- |
| `QuoteBuilder.module.css` | 108 | 129 | seq 678 |
| `QuoteBuilder.tsx` | 539 | 568 | seq 633, 651, 678 |
| `app/api/agent/catalog/[id]/route.ts` | 81 | 186 | seq 678 |
| `components/agent/CustomCatalogManager.tsx` | 49 | 114 | seq 678 |
| `…/quote/components/OptionCard.tsx` | — | 18 | seq 678, 689 (first touched after 628) |
| tracked files changed | 37 | 38 | +`OptionCard.tsx` |
| `git status --short` lines | 54 | 55 | +`OptionCard.tsx` |

## Recovered files

### Modified (38)

`.github/workflows/quality.yml`,
`app/agent/(app)/cases/CaseRowActions.tsx`,
`app/agent/(app)/cases/CasesList.tsx`,
`app/agent/(app)/cases/[caseId]/page.tsx`,
`app/agent/(app)/cases/page.tsx`,
`app/agent/(app)/estimates/page.tsx`,
`app/agent/(app)/meetings/[meetingId]/page.tsx`,
`app/agent/(app)/meetings/[meetingId]/quote/QuoteBuilder.module.css`,
`app/agent/(app)/meetings/[meetingId]/quote/QuoteBuilder.tsx`,
`app/agent/(app)/meetings/[meetingId]/quote/components/AgentEconomicsBlock.tsx`,
`app/agent/(app)/meetings/[meetingId]/quote/components/ExternalExpensesBlock.tsx`,
`app/agent/(app)/meetings/[meetingId]/quote/components/OptionCard.tsx`,
`app/agent/(app)/meetings/[meetingId]/quote/page.tsx`,
`app/api/agent/catalog/[id]/route.ts`,
`app/api/agent/catalog/route.ts`,
`app/api/agent/meeting/[meetingId]/quote/route.ts`,
`app/api/co/[code]/agree/route.ts`,
`app/api/co/[code]/route.ts`,
`app/co/[code]/CoView.module.css`,
`app/co/[code]/CoView.tsx`,
`app/co/[code]/page.tsx`,
`components/agent/CustomCatalogManager.tsx`,
`lib/apiAuth.ts`,
`lib/calculationUtils.ts`,
`lib/caseDomain.ts`,
`lib/caseReadModel.ts`,
`lib/caseService.ts`,
`lib/operationalAudit.ts`,
`lib/operationalAuth.ts`,
`lib/operationsProjection.ts`,
`package.json`,
`prisma/schema.prisma`,
`scripts/validate-mission-evidence.mjs`,
`tests/caseDomain.test.ts`,
`tests/e2e/m1-fixture.ts`,
`tests/integration/_setup.ts`,
`tests/integration/leads.itest.ts`,
`tests/integration/testDatabaseSafety.ts`

### Created (29)

`app/agent/(app)/presentations/[presentationId]/PresentationControls.tsx`,
`app/agent/(app)/presentations/[presentationId]/page.tsx`,
`app/api/agent/presentations/[presentationId]/route.ts`,
`app/api/agent/quotes/[quoteId]/link/route.ts`,
`app/api/agent/quotes/[quoteId]/presentation/route.ts`,
`app/api/agent/quotes/[quoteId]/publish/route.ts`,
`app/api/agent/quotes/[quoteId]/review/route.ts`,
`app/api/co/[code]/request-changes/route.ts`,
`docs/evidence/missions/M2/{mission.yaml,implementation.md,migration.md,security.md,ux-uat.md,review.md,release.md,test-results.json,acceptance.json}`,
`lib/commercialDraftAdapter.ts`,
`lib/commercialQuote.ts`,
`lib/commercialQuoteService.ts`,
`lib/commercialQuoteValidation.ts`,
`lib/commercialReconciliation.ts`,
`prisma/migrations/20260729170000_m2_commercial_trust_loop/migration.sql`,
`tests/commercialQuote.test.ts`,
`tests/e2e/m2-commercial.mjs`,
`tests/integration/commercialCatalog.itest.ts`,
`tests/integration/commercialQuotes.itest.ts`,
`tests/migrations/m2-commercial-assertions.sql`,
`tests/migrations/m2-commercial-fixtures.sql`

### Deleted

None. Sequences 122 and 136 issue `Delete File` + `Add File` pairs for
`app/api/co/[code]/route.ts`, `app/api/co/[code]/agree/route.ts` and
`app/co/[code]/CoView.tsx`; these are full rewrites, and all three files exist in the
recovered tree.

## State of the last, unconfirmed edit

Journal sequence 692 changed `createCommercialClientLink` in
`lib/commercialQuoteService.ts` from `runOperationalTransaction(async (tx) => { … })`
to `runOperationalTransaction(async (tx) => { … }, 5)`. Codex then ran `lint`,
`typecheck` and two commercial integration files (sequences 695–697) but the session
ended before any result was returned, so the edit is unverified by Codex.

Recovered state audit of that edit:

| Requirement | Finding |
| --- | --- |
| Retry limited to the idempotent client-link command | `lib/commercialQuoteService.ts:489` is the only `maxAttempts` override in the repository; the other eight `runOperationalTransaction` call sites keep the default | 
| Retry bounded | `maxAttempts = 5`, finite `for` loop in `lib/operationalTransaction.ts:8` |
| Only `P2034` retried | `lib/operationalTransaction.ts:14` rethrows anything that is not a `PrismaClientKnownRequestError` with code `P2034` |
| Other errors not masked | Unchanged `throw error`; `OperationalCommandError` raised inside the callback propagates on the first attempt |
| Row lock preserved | `SELECT "id" FROM "QuoteVersion" … FOR UPDATE` retained at `lib/commercialQuoteService.ts:452` |
| No transaction timeout fix | No `timeout` or `maxWait` option anywhere in the transaction path |
| Integration suite not serialized | `test:integration` still runs `node --import tsx --test tests/integration/*.itest.ts` with no `--test-concurrency` pin |

The remaining behavioural requirements (exactly one active link, idempotent replay,
concurrent distinct keys) are asserted by the integration suite; see `test-results.json`.
